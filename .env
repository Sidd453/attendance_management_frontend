# Optional: override the backend API base URL.
# In dev, Vite already proxies /api -> http://localhost:5000 (see vite.config.ts),
# so you usually don't need to set this locally.
VITE_API_URL=https://attendance-management-backend-n14u.onrender.com/api